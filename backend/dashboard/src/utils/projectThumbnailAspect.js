/** Matches homepage + /work project card thumbnails (400×420px). */
export const PROJECT_CARD_ASPECT = 400 / 420;
export const PROJECT_CARD_OUTPUT_WIDTH = 1200;
export const PROJECT_CARD_OUTPUT_HEIGHT = Math.round(
  PROJECT_CARD_OUTPUT_WIDTH / PROJECT_CARD_ASPECT,
);
